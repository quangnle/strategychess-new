{
  "name": "strategy-chess-offline",
  "version": "1.0.0",
  "description": "Strategy Chess Offline Mode",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "debug": "node --inspect server.js",
    "debug-brk": "node --inspect-brk server.js",
    "serve": "npx http-server -p 8000 -c-1 --cors",
    "test": "echo \"No tests specified\" && exit 0"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "keywords": ["game", "chess", "strategy", "offline"],
  "author": "Strategy Chess Team",
  "license": "MIT"
} 