import { BaseStrategy } from "../base-strategy.js";

export class NizzaStrategy extends BaseStrategy {
    // Nizza strategy inherits all common functionality from BaseStrategy
    // Override evaluatePosition if specific logic is needed
} 