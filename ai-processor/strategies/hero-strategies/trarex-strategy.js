import { BaseStrategy } from "../base-strategy.js";

export class TrarexStrategy extends BaseStrategy {
    // Trarex strategy inherits all common functionality from BaseStrategy
    // Override evaluatePosition if specific logic is needed
} 