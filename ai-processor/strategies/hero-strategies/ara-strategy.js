import { RangerStrategy } from "../ranger-strategy.js";

export class AraStrategy extends RangerStrategy {
    // Ara strategy inherits all common functionality from BaseStrategy
    // Override evaluatePosition if specific logic is needed
} 