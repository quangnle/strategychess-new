import { BaseStrategy } from "../base-strategy.js";

export class WizziStrategy extends BaseStrategy {
    // Wizzi strategy inherits all common functionality from BaseStrategy
    // Override evaluatePosition if specific logic is needed
} 